
| EXPRESSION                         | DESCRIPTION                                                                                                                                                   |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [[node.appendChild(new)]]          | Appends _new_ node as the last child of _node_                                                                                                                |
| [[node.insertBefore( new, child)]] | Insert _new_ node as a sibling directly before _child_ node (if no _child_ node is specified then _new_ node is added as a sibling after the last child node) |
| [[node.removeChild( old )]]        | Remove _old_ node from _node_                                                                                                                                 |
| [[node.replaceChild( new, old )]]  |                                                                                                                                                               |
