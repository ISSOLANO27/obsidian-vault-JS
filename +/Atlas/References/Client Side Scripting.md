
 HTML (Hypertext Markup Language) was never intended to control the appearance of pages in a `web browser.`
 
 [[Exploring the JavaScript Language]] provides the HTML needed in which Scripting Languages run on a local browser or the [[Client tier]] instead of on a Web Server. 

JS is NOT Java..
 Although Java can be used to create programs that can run from a web page, Java programs are usually external programs that execute independently of a browser.

> JavaScript was the answer in getting web pages to respond dynamically to user actions without putting extra strain on the server.

related:










