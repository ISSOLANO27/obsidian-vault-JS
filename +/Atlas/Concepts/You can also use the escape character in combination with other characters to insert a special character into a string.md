The backslash followed by an apostrophe ( \' ) and the backslash followed by a double quotation mark ( \" ) are both examples of [[escape sequence]]s. 

Most escape sequences carry out special functions. 

For example, the escape sequence `\t` inserts a tab into a string.  describes the escape sequences that can be added to a string in JavaScript.

