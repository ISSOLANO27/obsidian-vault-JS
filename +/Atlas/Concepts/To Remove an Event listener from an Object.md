apply the following command:
```js
object.removeEventListner("event", function, capture)
```
Once the event listener has been removed, the event will no longer trigger a response from the script.

Back: [[To Attach an Event Listener to an object]]