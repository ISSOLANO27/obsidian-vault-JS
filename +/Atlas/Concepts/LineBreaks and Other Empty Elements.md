`<br />`
These `Line Brakes` are placed within #groupingElements  like `<p> and <h1>.`

The img element is inserted using the empty element tag because it does not enclose any text content, but instead links to an external image file.
#emptyElements 


>Although it may be useful it should not be used as a formatting tool. You would not want to use two of them back to back just to increase the spacing. 

Instead use a stylesheet.

Don't worry if the text line cannot fit within the width of the viewing window, the browser will automatically wrap the text at where it find it appropriate.

---
`<wbr />`
To recommend a different break point, use `wbr (wordbreak)` element to indicate where a line break should occur if needed.

```html
www.curbsidethai.com/<wbr />general/docs/ct_about.html
```


[[INSIGHT - Insight Supporting HTML 5 with Legacy Browsers]]

---

Related:
- [[HTML Compatibility Tools]]
- [[Using Text Level Element]]
- [[wbr]]
- [[hr]]








