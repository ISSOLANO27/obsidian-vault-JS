It is not shown within the web page but rather in the browser title bar/browser tab.

```html
<head>
<title>Document title</title>
<head>
```

> Tip: Document titles **should be no more than 64 characters** in length to ensure that the text fits on the browser title bar or a browser tab.

Related:
- [[Creating The Document Head]]
