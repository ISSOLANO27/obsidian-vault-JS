Array values are left undefined so that the length of the array is greater than the number of defined values. 

The following commands create a sparse array with only two defined values out of 100:
```js
let x = New Array();
x[0] = "Aaron";
x[99] = "Zukov";
```

---
Back: [[Elements and Indexes]]