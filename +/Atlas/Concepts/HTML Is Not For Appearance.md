> It only describes the document's content and structure.

Since the same document could be rendered differently on different devices or screen sizes we use [[Linking an HTML Document to a Style Sheet]] to determine the appearance. 

