The general syntax is:

```js
condition ? trueValue : condition2 ? : trueValue : falseValue;
```

where the expression returns _trueValue_ if _condition1_ is _true_ or _condition2_ is _true_ ; otherwise it returns _falseValue_ .