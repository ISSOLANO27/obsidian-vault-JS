
Browser JS cannot do:

`run("format C:")`
`open("cmd.exe")`
`delete("/home/user/documents")`

That ability simply does not exists in the browser runtime.

This prevents catastrophic damage.

>Node.js is is NOT the case.. (look into it)

related:
- [[JavaScript Limitations]]