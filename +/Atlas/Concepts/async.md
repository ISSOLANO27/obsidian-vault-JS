tells the browser to parse the HTML and JavaScript code together, only pausing to process the script before returning to the HTML file.

[[Great for analytics scripts.  ]]
[[Bad for DOM manipulation scripts]].

![[Loading HTML and JavaScript code.png]]

related:
- [[Creating a JS Source File]]