Under JavaScript’s event model, an event like click is first tracked in the **capture phase**,  #capturedPhase moving down the object hierarchy from the most general object (the browser window) down to the specific (the image itself).

