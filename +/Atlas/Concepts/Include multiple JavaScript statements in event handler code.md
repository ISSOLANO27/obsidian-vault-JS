as long as semicolons separate the statements.

a statement that creates a variable and another statement that uses the `window.alert()` method to display the variable; you would type the following:

```js
<input type="submit"
       onclick="let msg = 'Thanks for your order! We appreciate your business.';
                window.alert(msg);" />

```

related:
- [[1. As An Inline Attribute (HTML Level)]]