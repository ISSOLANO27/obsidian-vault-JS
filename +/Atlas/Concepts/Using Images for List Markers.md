`list-style-image: url(url);`

You can supply your own image as a list marker.

Marker images are **only used with unordered lists** in which the list marker is the same for every list item.
```css
ul {list-style-image: url(redball.png);}
```

---
Back: [[Formatting List]]

