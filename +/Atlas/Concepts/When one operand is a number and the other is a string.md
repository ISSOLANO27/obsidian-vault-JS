JavaScript interpreters attempt to convert the string value to a number. 

If the string value **cannot** be converted to a number, a value of `false` is returned. 

For example, the expression `10 === "ten";` returns a value of `false` because **JavaScript interpreters cannot convert the string “ten” to a number**.

---
back: [[Comparison Operators]]