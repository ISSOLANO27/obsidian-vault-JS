Browser stops loading the page and processes the statements enclosed within the script.

Once the script is run, the browser continues to process the remaining content in the HTML file.

related:
- [[The Script Element]]