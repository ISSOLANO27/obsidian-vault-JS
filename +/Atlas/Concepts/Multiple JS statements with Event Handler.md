You can include **multiple JavaScript statements** with the event handler as in the following example in which both the `calcTotal(`) and `writeMsg()` functions are called.
```js
<input type="button" id="total" value="Calculate" onClick="calcTotal(); writeMsg()" />
```

---
Back: [[Using Event Handlers]]