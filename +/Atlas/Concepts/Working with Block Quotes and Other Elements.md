```html
<blockquote>content</blockquote>
```

content is the quote.

by default, most browsers indent the quoted material to separate it from the website author's words.

you can sub out the default styling using a custom stylesheet.