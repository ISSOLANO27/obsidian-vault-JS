The entire structure of an HTML document can be thought of as **a set of nested elements in a hierarchical tree**. 

At the top of the tree is the html element marking the entire document. 

Within the html element is the **head element** enclosing information about the document itself and the **body element** enclosing the content of the web page. 


[[Introducing Element Attributes]]
[[ Creating the Basic Structure of an HTML File]]
[[Document Object Model (DOM)]]

back:
[[Exploring an HTML Document]]



