In the following code, both functions will be run in response to the **load** event of the window object:
```js
window.addEventListner("load", function1);
window.addEventListner("load", function2);
```