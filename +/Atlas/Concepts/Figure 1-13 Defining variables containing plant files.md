![[Pasted image 20260215141458.png]]
Notice that the src attribute for the img element is using `#` as the value. This is so that the **default is to not display any image at all.**

> Later we will replace the src attribute value with one of the variable values i will define in this script so that the image changes in response to user actions.

related:
- [[Declaring and Initializing Variables]]