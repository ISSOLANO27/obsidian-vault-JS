### To clip an element’s content, use the property:
```css
clip: rect(top, right, bottom, left);
```
where _top_, _right_, _bottom_, and _left_ define the coordinates of the clipping rectangle.

### To remove clipping for a clipped object, use:
```css
clip: auto;
```

---
Back: [[Clipping an Element]]