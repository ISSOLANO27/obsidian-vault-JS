calcTotal() would be run **in response to** the input button being clicked by the user:

For example:
```html
<input type="button" id="total" value="Calculate" onClick="calcTotal()" />
```

---
Back: [[Using Event Handlers]]