An **assignment operator** is used for assigning a value to a variable. The most common assignment operator is the equal sign (`=`), but JavaScript supports other operators known as [[Compound assignment operators]].


Both the compound sign operators. And regular assignment operators performed the addition operation and the signs of values that this expression is equivalent to...
```js
x = x + 5
```

---
back: [[Using Operators to Build Expression]]