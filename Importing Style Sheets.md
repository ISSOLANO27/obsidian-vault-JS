The `@charset` rule is an example of a [[CSS at-rule]].

Another CSS at-rule is the following [[@import]] used to import the contents of a style sheet file:
```css
@import url(url);
```


> The `@import` statement **MUST** always come before any other style rules in the style sheet !

url - the URL of an external sheet file.


